import type { Message } from '../../../types';
interface MensagemChatProps {
    mensagem: Message;
}
export declare const MensagemChat: ({ mensagem }: MensagemChatProps) => import("react/jsx-runtime").JSX.Element;
export {};
